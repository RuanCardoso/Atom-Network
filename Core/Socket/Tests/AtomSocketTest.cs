using Atom.Core;
using UnityEngine;

public class AtomSocketTest : MonoBehaviour
{
    private readonly AtomStream _atomStream = new();

    private void Awake()
    {
        //atomStream = new AtomStream();
    }

    private void Start()
    {

    }

    private void Update()
    {
        //_atomStream.Write(1);
        //_atomStream.Write((short)2);
        //_atomStream.Write((byte)3);
        //_atomStream.Write((float)4);
        //_atomStream.Write((double)5);
        //_atomStream.Position = 0;
        //_atomStream.Read(out int value);
        //Debug.Log(value);
        //_atomStream.Read(out short value2);
        //Debug.Log(value2);
        //_atomStream.Read(out byte value3);
        //Debug.Log(value3);
        //_atomStream.Read(out float value4);
        //Debug.Log(value4);
        //_atomStream.Read(out double value5);
    }
}